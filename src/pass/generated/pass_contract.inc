// Generated by python/tools/generate_pass_contract.py; do not edit.
#pragma once

#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include "kxc/pass/pass.h"

namespace kxc::pass_contract_generated {

inline constexpr int kContractVersion = 2;

inline const std::vector<PassSpec>& Specs() {
    static const std::vector<PassSpec> specs = [] {
        std::vector<PassSpec> result;
        {
            PassSpec spec;
            spec.name = String("fold_tuple_get_item");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.fold_tuple_get_item");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("fold_constant");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.fold_constant");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("simplify_expr");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.simplify_expr");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("canonicalize_cast");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.canonicalize_cast");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("remove_standalone_reshapes");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.remove_standalone_reshapes");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("eliminate_common_subexpr");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 3;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.eliminate_common_subexpr");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("eliminate_dead_let");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.eliminate_dead_let");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("annotate_memory_scope");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.annotate_memory_scope");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("capture_post_dfs_index_in_spans");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.capture_post_dfs_index_in_spans");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("infer_type");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("relay");
            spec.scope = ParsePassScope("graph");
            spec.phase = String("relay_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {String("checked_type")};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.relay.transform.infer_type");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("fold_constant");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.fold_constant");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("simplify_expr");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.simplify_expr");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("force_narrow_index_to_i32");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.force_narrow_index_to_i32");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("convert_for_loops_serial");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.convert_for_loops_serial");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("loop_partition");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.loop_partition");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("unroll_loop");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.unroll_loop");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("vectorize_loop");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.vectorize_loop");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("remove_no_op");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_optimize");
            spec.opt_level = 1;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = true;
            spec.thread_safe = false;
            spec.target_dependent = false;
            spec.implementation_key = String("kxc.tir.transform.remove_no_op");
            result.push_back(std::move(spec));
        }
        {
            PassSpec spec;
            spec.name = String("bind_cuda_threads");
            spec.schema_version = 1;
            spec.dialect = ParseIRDialect("tir");
            spec.scope = ParsePassScope("prim_func");
            spec.phase = String("tir_schedule");
            spec.opt_level = 3;
            spec.required_invariants = {};
            spec.produced_invariants = {};
            spec.declarative_only_invariants = {};
            spec.preserved_analyses = {};
            spec.invalidated_analyses = {};
            spec.may_change_ir = true;
            spec.deterministic = true;
            spec.idempotent = false;
            spec.thread_safe = false;
            spec.target_dependent = true;
            spec.implementation_key = String("kxc.tir.transform.bind_cuda_threads");
            result.push_back(std::move(spec));
        }
        return result;
    }();
    return specs;
}

inline Array<PassSpec> Specs(IRDialect dialect) {
    Array<PassSpec> result;
    for (const PassSpec& spec : Specs()) {
        if (spec.dialect == dialect) result.push_back(spec);
    }
    return result;
}

inline Array<String> Pipeline(const std::string& name) {
    if (name == "relay.optimize_default") return {String("fold_tuple_get_item"), String("fold_constant"), String("simplify_expr"), String("canonicalize_cast"), String("remove_standalone_reshapes"), String("eliminate_dead_let"), String("annotate_memory_scope"), String("capture_post_dfs_index_in_spans"), String("infer_type")};
    if (name == "tir.optimize_default") return {String("fold_constant"), String("simplify_expr"), String("force_narrow_index_to_i32"), String("convert_for_loops_serial"), String("loop_partition"), String("unroll_loop"), String("vectorize_loop"), String("remove_no_op")};
    if (name == "relay.compiler.o0") return {};
    if (name == "relay.compiler.o1") return {String("fold_tuple_get_item"), String("fold_constant"), String("simplify_expr")};
    if (name == "relay.compiler.o2") return {String("fold_tuple_get_item"), String("fold_constant"), String("simplify_expr"), String("canonicalize_cast"), String("remove_standalone_reshapes"), String("eliminate_dead_let")};
    if (name == "relay.compiler.o3") return {String("fold_tuple_get_item"), String("fold_constant"), String("simplify_expr"), String("canonicalize_cast"), String("remove_standalone_reshapes"), String("eliminate_dead_let")};
    if (name == "tir.compiler.o0") return {};
    if (name == "tir.compiler.o1") return {String("fold_constant"), String("simplify_expr")};
    if (name == "tir.compiler.o2") return {String("fold_constant"), String("simplify_expr"), String("force_narrow_index_to_i32"), String("convert_for_loops_serial")};
    if (name == "tir.compiler.cpu.o3") return {String("fold_constant"), String("simplify_expr"), String("force_narrow_index_to_i32"), String("convert_for_loops_serial"), String("loop_partition"), String("unroll_loop"), String("vectorize_loop"), String("remove_no_op")};
    if (name == "tir.compiler.cuda.o3") return {String("fold_constant"), String("simplify_expr"), String("force_narrow_index_to_i32"), String("remove_no_op"), String("bind_cuda_threads")};
    throw std::invalid_argument("unknown generated pipeline: " + name);
}

}  // namespace kxc::pass_contract_generated
