// Generated by python/tools/generate_relay_op_contract.py; do not edit.
#pragma once

#include <stdexcept>
#include <string>

#include "kxc/relay/op.h"

namespace kxc::relay::op_contract_generated {

inline constexpr int kContractVersion = 1;

inline OperatorSpec Spec(const std::string& name) {
    if (name == "add") {
        OperatorSpec spec;
        spec.name = "add";
        spec.schema_version = 1;
        spec.category = "tensor.math";
        spec.input_arity.num_inputs = 2;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "cast") {
        OperatorSpec spec;
        spec.name = "cast";
        spec.schema_version = 1;
        spec.category = "tensor.transform";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "CastAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "divide") {
        OperatorSpec spec;
        spec.name = "divide";
        spec.schema_version = 1;
        spec.category = "tensor.math";
        spec.input_arity.num_inputs = 2;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "matmul") {
        OperatorSpec spec;
        spec.name = "matmul";
        spec.schema_version = 1;
        spec.category = "tensor.math";
        spec.input_arity.num_inputs = 2;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "mul") {
        OperatorSpec spec;
        spec.name = "mul";
        spec.schema_version = 1;
        spec.category = "tensor.math";
        spec.input_arity.num_inputs = 2;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_avg_pool2d") {
        OperatorSpec spec;
        spec.name = "nn_avg_pool2d";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "MaxPool2DAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_conv2d") {
        OperatorSpec spec;
        spec.name = "nn_conv2d";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = -1;
        spec.input_arity.min_inputs = 2;
        spec.input_arity.max_inputs = 3;
        spec.attrs_type_key = "Conv2DAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_dense") {
        OperatorSpec spec;
        spec.name = "nn_dense";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = 2;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "DenseAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_flatten") {
        OperatorSpec spec;
        spec.name = "nn_flatten";
        spec.schema_version = 1;
        spec.category = "tensor.transform";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "FlattenAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_gemm") {
        OperatorSpec spec;
        spec.name = "nn_gemm";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = 3;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "GemmAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_global_avg_pool2d") {
        OperatorSpec spec;
        spec.name = "nn_global_avg_pool2d";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "GlobalAvgPool2DAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_max_pool2d") {
        OperatorSpec spec;
        spec.name = "nn_max_pool2d";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "MaxPool2DAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "nn_relu") {
        OperatorSpec spec;
        spec.name = "nn_relu";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "ReluAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "reduce_mean") {
        OperatorSpec spec;
        spec.name = "reduce_mean";
        spec.schema_version = 1;
        spec.category = "tensor.reduce";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "ReduceMeanAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "reshape") {
        OperatorSpec spec;
        spec.name = "reshape";
        spec.schema_version = 1;
        spec.category = "tensor.transform";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "ReshapeAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "softmax") {
        OperatorSpec spec;
        spec.name = "softmax";
        spec.schema_version = 1;
        spec.category = "nn";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "SoftmaxAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "sqrt") {
        OperatorSpec spec;
        spec.name = "sqrt";
        spec.schema_version = 1;
        spec.category = "tensor.math";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "subtract") {
        OperatorSpec spec;
        spec.name = "subtract";
        spec.schema_version = 1;
        spec.category = "tensor.math";
        spec.input_arity.num_inputs = 2;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    if (name == "transpose") {
        OperatorSpec spec;
        spec.name = "transpose";
        spec.schema_version = 1;
        spec.category = "tensor.transform";
        spec.input_arity.num_inputs = 1;
        spec.input_arity.min_inputs = -1;
        spec.input_arity.max_inputs = -1;
        spec.attrs_type_key = "TransposeAttrs";
        spec.output_arity = 1;
        spec.type_relation_key = "FInferType";
        spec.effect = OperatorEffectKind::kPure;
        spec.deterministic = true;
        spec.alias_contract = "none";
        spec.lowering_kind = OperatorLoweringKind::kSingleTE;
        spec.lowering_key = "FRelayToTE";
        return spec;
    }
    throw std::invalid_argument("operator is absent from generated contract: " + name);
}

}  // namespace kxc::relay::op_contract_generated
